name: Test github action

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: "closed"
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: say hello
        run: echo "hello"
